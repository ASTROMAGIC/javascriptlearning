const birthYear = 1948; 
const thisYear = 1965; 
const firstName = "Carlos"; 
const lastName = "Stevenson";

const greeting = "Hello! My name is " + firstName + " " + lastName + " and I am " + (thisYear - birthYear) + " years old. "; 

// this is four variables being called, with a final variable combining them together into a greeting using string concatenation. 

// the outcome should be that the person tells you who they are and how old they are. 

console.log(greeting); 